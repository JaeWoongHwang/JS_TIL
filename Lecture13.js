// String 이어 붙이기
// 1. .concat 함수 사용 (concatenate의 약어)
// 2. 더하기(+)연산자 사용

var str  = "Hello";
str.length;
str["length"];
var str2 = "World";
str.concat(str2):
var str3 = str.concat(str2);
str.concat(str2).concat("!");
"Hello".concat("World").concat("!");
"Hello".length;
str+str2;
"Hello"+"World";

// 문자열과 숫자를 더하면 숫자를 문자열로 변환하고 더한 결과를 출력해준다
"PI is" + 3.14;
3.14 + "is PI"
